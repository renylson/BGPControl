# BGPControl - Configuração de Ambiente
# 
# Desenvolvido por: Renylson Marques
# E-mail: renylsonm@gmail.com
#
# Este arquivo contém todas as configurações necessárias para o BGPControl.
# Renomeie para 'config.env' e ajuste os valores conforme seu ambiente.
#
# SEGURANÇA: Nunca commite este arquivo com senhas reais no Git!

# =============================================================================
# CONFIGURAÇÕES DE INSTALAÇÃO AUTOMATIZADA
# =============================================================================

# Tipo de acesso ao sistema
# 1 = Acesso via IP (desenvolvimento/rede interna)
# 2 = Acesso via domínio (produção com DNS)
ACCESS_TYPE=1

# Domínio para acesso (obrigatório se ACCESS_TYPE=2)
# Exemplo: bgpcontrol.empresa.com
DOMAIN=""

# Configurar SSL/HTTPS automaticamente
# true = Configurar Let's Encrypt (requer domínio válido)
# false = HTTP apenas (não recomendado para produção)
USE_SSL=false

# E-mail do administrador para certificados SSL
# Necessário apenas se USE_SSL=true
ADMIN_EMAIL="renylsonm@gmail.com"

# =============================================================================
# CONFIGURAÇÕES DO BANCO DE DADOS POSTGRESQL
# =============================================================================

# Senha do usuário PostgreSQL
# IMPORTANTE: Use uma senha forte em produção
# Deixe vazio para gerar automaticamente uma senha segura
DB_PASSWORD="BGPControl_SecureDB_2024!"

# Nome do banco de dados
DB_NAME="bgpcontrol"

# Usuário do banco de dados
DB_USER="bgpcontrol"

# Host do banco (padrão: localhost para instalação local)
DB_HOST="localhost"

# Porta do PostgreSQL (padrão: 5432)
DB_PORT="5432"

# =============================================================================
# CONFIGURAÇÕES DO USUÁRIO ADMINISTRADOR INICIAL
# =============================================================================

# Nome de usuário do administrador
ADMIN_USERNAME="admin"

# Senha do administrador
# IMPORTANTE: Altere esta senha após o primeiro login
ADMIN_PASSWORD="BGPControl@Admin2024"

# Nome completo do administrador
ADMIN_NAME="Renylson Marques"

# E-mail do administrador
ADMIN_EMAIL_USER="renylsonm@gmail.com"
# =============================================================================
# CONFIGURAÇÕES AVANÇADAS DO SISTEMA
# =============================================================================

# Diretório de instalação do sistema
# Padrão: /opt/bgpcontrol (recomendado para produção)
INSTALL_DIR="/opt/bgpcontrol"

# Usuário do sistema para execução dos serviços
# Criado automaticamente se não existir
SERVICE_USER="bgpcontrol"

# Instalar e configurar Nginx como reverse proxy
# true = Recomendado para produção
# false = Para desenvolvimento ou se já tiver proxy configurado
INSTALL_NGINX=true

# Configurar firewall UFW automaticamente
# true = Abrir apenas portas necessárias (80, 443, 22)
# false = Manter configuração atual do firewall
SETUP_FIREWALL=true

# Habilitar backup automático
# true = Configurar cron job para backup diário
# false = Backup manual apenas
ENABLE_AUTO_BACKUP=true

# Diretório para backups
BACKUP_DIR="/opt/bgpcontrol/backups"

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =============================================================================

# Chave secreta JWT (deixe vazio para gerar automaticamente)
# IMPORTANTE: Use uma chave forte em produção
JWT_SECRET=""

# Algoritmo JWT (padrão: HS256)
JWT_ALGORITHM="HS256"

# Tempo de expiração do token em minutos (padrão: 1440 = 24 horas)
JWT_EXPIRE_MINUTES=1440

# Habilitar CORS (Cross-Origin Resource Sharing)
# Necessário se frontend estiver em domínio diferente
ENABLE_CORS=true

# Origins permitidas para CORS (separadas por vírgula)
# "*" permite todas as origins (não recomendado para produção)
CORS_ORIGINS="*"

# =============================================================================
# CONFIGURAÇÕES DE APLICAÇÃO
# =============================================================================

# Porta do backend FastAPI (padrão: 8000)
BACKEND_PORT=8000

# Porta do frontend (padrão: 3000, usado apenas em desenvolvimento)
FRONTEND_PORT=3000

# Modo debug (true/false)
# ATENÇÃO: Sempre false em produção
DEBUG=false

# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL="INFO"

# Diretório de logs
LOG_DIR="/var/log/bgpcontrol"

# =============================================================================
# CONFIGURAÇÕES DE E-MAIL (OPCIONAL)
# =============================================================================

# Configurações para envio de alertas por e-mail
# Deixe vazio se não quiser configurar e-mail

# Servidor SMTP
SMTP_HOST=""

# Porta SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587

# Usuário SMTP
SMTP_USER=""

# Senha SMTP
SMTP_PASSWORD=""

# Usar TLS/SSL (true/false)
SMTP_USE_TLS=true

# E-mail remetente
MAIL_FROM="bgpcontrol@empresa.com"

# =============================================================================
# EXEMPLOS DE CONFIGURAÇÃO POR AMBIENTE
# =============================================================================

# DESENVOLVIMENTO LOCAL:
# ACCESS_TYPE=1
# USE_SSL=false
# DEBUG=true
# LOG_LEVEL="DEBUG"
# SETUP_FIREWALL=false

# PRODUÇÃO COM IP:
# ACCESS_TYPE=1
# USE_SSL=false
# DEBUG=false
# LOG_LEVEL="INFO"
# SETUP_FIREWALL=true
# ENABLE_AUTO_BACKUP=true

# PRODUÇÃO COM DOMÍNIO E SSL:
# ACCESS_TYPE=2
# DOMAIN="bgpcontrol.empresa.com"
# USE_SSL=true
# ADMIN_EMAIL="admin@empresa.com"
# DEBUG=false
# LOG_LEVEL="INFO"
# SETUP_FIREWALL=true
# ENABLE_AUTO_BACKUP=true

# =============================================================================
# NOTAS DE SEGURANÇA
# =============================================================================

# 1. Sempre altere as senhas padrão após a instalação
# 2. Use senhas fortes (mínimo 12 caracteres, letras, números e símbolos)
# 3. Mantenha este arquivo seguro (permissões 600)
# 4. Nunca commite senhas reais no controle de versão
# 5. Em produção, considere usar secrets management (HashiCorp Vault, etc.)
# 6. Faça backup regular das configurações e dados
# 7. Monitore logs de acesso e erros regularmente

# =============================================================================
# COMANDOS ÚTEIS PÓS-INSTALAÇÃO
# =============================================================================

# Verificar status dos serviços:
# sudo systemctl status bgpcontrol-backend
# sudo systemctl status nginx
# sudo systemctl status postgresql

# Ver logs em tempo real:
# sudo journalctl -u bgpcontrol-backend -f
# sudo tail -f /var/log/nginx/access.log

# Fazer backup manual:
# curl -X POST http://localhost:8000/api/backup/create -H "Authorization: Bearer TOKEN"

# Verificar conectividade:
# curl http://localhost:8000/health
# curl http://localhost/api/health
# DOMAIN="bgpcontrol.exemplo.com.br"
# USE_SSL=true
# ADMIN_EMAIL="admin@exemplo.com.br"
# ADMIN_USERNAME="admin"
# ADMIN_PASSWORD="SenhaSegura123!"
# ADMIN_NAME="Administrador BGP"

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
#
# 1. Senhas devem ter pelo menos 8 caracteres
# 2. Para produção, use senhas fortes e únicas
# 3. Se DB_PASSWORD estiver vazio, será gerada automaticamente
# 4. Se JWT_SECRET estiver vazio, será gerado automaticamente
# 5. Para usar SSL, o domínio deve estar apontando para o servidor
# 6. Este arquivo contém informações sensíveis - proteja adequadamente
#
